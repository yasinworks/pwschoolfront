import React from 'react';
import './Header.scss';

function Header(props) {
    return (
        <div className={'header'}>
            <div className={'header_user-wrap'}></div>
        </div>
    );
}

export default Header;